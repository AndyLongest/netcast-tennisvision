# Historical notes

This directory records why earlier experiments were accepted or rejected. It is not a
runtime fallback directory. Large videos and caches from those experiments were pruned;
current executable behavior is documented in `../CURRENT_ARCHITECTURE.md` and
`../RACKETVISION_PRODUCTION.md`.
